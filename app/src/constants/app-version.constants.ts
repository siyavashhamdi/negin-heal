import packageJson from "../../package.json";

/** User-facing app version label shown on the More page. */
export const APP_VERSION = packageJson.version;

/** Delay before showing deploy metadata on version hover/touch. */
export const APP_VERSION_DEPLOY_TOOLTIP_DELAY_MS = 5000;
