export enum UserRole {
  SUPER_ADMIN = "SUPER_ADMIN", // Super Admin - Platform-level admin for global app settings (billing, config, system)
  ADMIN = "ADMIN", // ادمین - Admin
  END_USER = "END_USER", // کاربر نهایی - End User
}
